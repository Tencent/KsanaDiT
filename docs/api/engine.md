# Engine

The `Engine` is the central orchestrator of kDiT. It manages the lifecycle of model loading and inference execution.

::: kdit.engine.Engine
    options:
      members:
        - get_default
        - reset_default
        - load
        - infer
        - generate
